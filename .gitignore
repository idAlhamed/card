# ---------------------------------------------------------------------------
# Reference material — MUST NOT be published.
# IMG_3890.jpg contains a real Adatwq ID number and a personal QR code.
# The screenshot is another designer's work, kept only as visual reference.
# Both files stay on disk untouched; they are simply never committed.
# ---------------------------------------------------------------------------
IMG_3890.jpg
Screenshot*.png

# ---------------------------------------------------------------------------
# Apple Wallet signing material — never commit. These are credentials.
# ---------------------------------------------------------------------------
wallet/certs/
*.p12
*.pem
*.cer
*.certSigningRequest

# Pass build artifacts (regenerated by `npm run pass:sign`)
wallet/**/manifest.json
wallet/**/signature
*.pkpass
dist/

# Node
node_modules/
npm-debug.log*

# macOS
.DS_Store

# Internal build/process reports — not part of the deliverable
.superpowers/
